<!DOCTYPE html>
<html lang="fr" class="font-grotesque bg-principal-800">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - TeckelCard</title>
    <link href="/css/output.css" rel="stylesheet"> <!-- Ici on utilise ton Tailwind compilé -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Darker+Grotesque:wght@300..900&display=swap" rel="stylesheet">
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>

<body class="flex justify-center items-center h-screen gap-7">
    <div class="flex justify-center items-center h-screen">
        <div class="flex flex-col justify-center items-center h-screen">
            <h2 class="text-[200px] font-bold mb-10 text-center text-gray-800 uppercase">login PAGE</h2>
            <div class="button-mint">
                <div>
                    <a href="/inscription">Première fois ici ?</a> <!-- Lien vers la page d'inscription -->
                </div>
            </div>
        </div>

        <form method="POST" action="/connexion" id="login-form" class="px-12 pt-10 pb-10 mb-4 w-full max-w-2xl">

            <!-- Token CSRF -->
            <input type="hidden" name="csrf_token" value="<%= csrf_token %>">

            <!-- Champ email -->
            <div class="mb-8">
                <label class="block text-text-dark text-xl font-bold mb-3" for="email">
                    E-mail
                </label>
                <input
                    class="shadow bg-mint-green-100 appearance-none border-3 rounded-lg w-full py-4 px-5 text-lg text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
                    type="email" id="email" name="email" required placeholder="Votre email">
            </div>

            <!-- Champ mot de passe -->
            <div class="mb-8">
                <label class="block text-text-dark text-xl font-bold mb-3" for="password">
                    Password
                </label>
                <input
                    class="shadow bg-mint-green-100 appearance-none border-3 rounded-lg w-full py-4 px-5 text-lg text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
                    type="password" id="password" name="motdepasse" required minlength="6"
                    placeholder="Votre mot de passe">
            </div>

            <div class="flex items-center mb-4">
                <input type="checkbox" id="remember" name="remember" class="mr-2">
                <label for="remember" class="text-gray-700">Rester connecté</label>
              </div>

            <!-- Bouton soumettre -->
            <div class="button-mint-xl">
                <div>
                    <button type="submit" class="flex justify-center items-center w-full h-full text-2xl font-bold text-black uppercase;">Connexion</button>
                </div>
            </div>

        </form>

    </div>
</body>

</html>
